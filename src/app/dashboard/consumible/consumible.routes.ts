import { Route } from '@angular/router';
import { ConsumibleComponent } from './index';

export const ConsumibleRoutes: Route[] = [
    {
      path: 'consumible',
      component: ConsumibleComponent
    }
];
 