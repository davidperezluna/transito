import { Route } from '@angular/router';
import { TipoIdentificacionComponent } from '.';

export const TipoIdentificacionRoutes: Route[] = [
    {
      path: 'tipoIdentificacion',
      component: TipoIdentificacionComponent
    }
];
 