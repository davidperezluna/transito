import { Route } from '@angular/router';
import { CombustibleComponent } from '.';

export const CombustibleRoutes: Route[] = [
    {
      path: 'combustible',
      component: CombustibleComponent
    }
];
 