import { Route } from '@angular/router';
import { SucursalComponent } from './index';

export const SucursalRoutes: Route[] = [
    {
      path: 'sucursal',
      component: SucursalComponent
    }
];
 