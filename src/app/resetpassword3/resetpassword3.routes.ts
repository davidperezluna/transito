import { Route } from '@angular/router';
import { Resetpassword3Component } from './index';

export const Resetpassword3Routes: Route[] = [
    {
      path: 'resetpassword3',
      component: Resetpassword3Component
    }
];
