import { Route } from '@angular/router';
import { Resetpassword4Component } from './index';

export const Resetpassword4Routes: Route[] = [
    {
      path: 'resetpassword4',
      component: Resetpassword4Component
    }
];
