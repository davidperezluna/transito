import { Route } from '@angular/router';
import { Resetpassword5Component } from './index';

export const Resetpassword5Routes: Route[] = [
    {
      path: 'resetpassword6',
      component: Resetpassword5Component
    }
];
