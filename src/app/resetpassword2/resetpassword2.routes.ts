import { Route } from '@angular/router';
import { Resetpassword2Component } from './index';

export const Resetpassword2Routes: Route[] = [
    {
      path: 'resetpassword2',
      component: Resetpassword2Component
    }
];
