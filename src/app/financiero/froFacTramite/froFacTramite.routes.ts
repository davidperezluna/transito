import { Route } from '@angular/router';
import { FroFacTramiteComponent } from '.';

export const FroFacTramiteRoutes: Route[] = [
    {
      path: 'froFacTramite',
      component: FroFacTramiteComponent
    }
];
 