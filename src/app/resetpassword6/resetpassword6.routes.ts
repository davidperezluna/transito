import { Route } from '@angular/router';
import { Resetpassword6Component } from './index';

export const Resetpassword6Routes: Route[] = [
    {
      path: 'resetpassword6',
      component: Resetpassword6Component
    }
];
